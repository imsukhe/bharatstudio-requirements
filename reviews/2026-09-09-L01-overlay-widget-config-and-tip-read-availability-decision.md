# Decision — L01 overlay widget availability classification

**State:** `Accepted for local completion; external deployment evidence pending`

An unconfigured or failed server-side read dependency is operationally
distinct from a missing bearer credential. This approves that error-boundary
correction and explicit top-level config projection only; free-form config
contract design remains deferred.

Review confirms the route boundary distinguishes credentials from operational
availability and retains the existing browser-facing behavior. Full local
verification exits 0; no live dependency or deployment conclusion is implied.
