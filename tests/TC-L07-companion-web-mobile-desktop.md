# L07 acceptance and test record — Companion web, mobile and native desktop helper

**Status:** `Core Web/API/mobile layout and macOS scaffold slices passing — Windows/store/device evidence pending`
**Task:** [`../tasks/L07-companion-web-mobile-desktop.md`](../tasks/L07-companion-web-mobile-desktop.md)
**Repositories:**
- `/Users/sukhdevsingh/Workspace/Bharat Studio/bharatstudio-alerts` (Web Companion/API contracts)
- `/Users/sukhdevsingh/Workspace/Bharat Studio/bharatstudio-companion-mobile` (React Native iOS/Android)
- `/Users/sukhdevsingh/Workspace/Bharat Studio/bharatstudio-companion-desktop` (WinUI 3/C# and SwiftUI/Swift)

| ID | Setup/action | Expected result | Evidence |
|---|---|---|---|
| L07-01 | Sign in on Web, iOS, Android, Windows and macOS with the same Google identity | Only the authorised account/channels load; no YouTube scope or channel-data request is made | Device matrix, auth scope capture; not run |
| L07-02 | Exercise the dedicated Web Companion Console as owner/operator and viewer across one and multiple authorised channels | The console uses the documented server state/action routes, lets an authorised user select a channel and active queue, sends the selected queue ID as the command target, disables controls without a valid target, exposes finite controls only to operator roles, and has no direct OBS/localhost command path | `bharatstudio-alerts/apps/web/app/companion/page.tsx`, `apps/web/app/lib/api.ts`, `TopNav.tsx`; unauthenticated browser smoke on `http://localhost:3100/companion` showed safe Authentication-required state and no console errors on 2026-08-15; authenticated multi-channel/API evidence pending |
| L07-02a | Submit each v1 Companion action with and without a queue target; retry `send_test_alert` with the same idempotency key | Supported actions require a target; test alert produces one durable event/outbox/delivery and retry returns the same event without duplication | API route test; L03 PostgreSQL harness; pass locally on 2026-08-15 |
| L07-03 | Pair an approved co-host/moderator path, revoke it, and retry after expiry | Pairing is short-lived, role-scoped, auditable and cannot grant owner credentials or unrestricted control | Pairing/revocation trace; not run |
| L07-04 | Try queue creation above the server-provided `queueCount` entitlement, configure action slots above the server-provided 8/16/32 tier allocation, select 4/8/16 page sizes, and try arbitrary Companion commands | Server rejects only the new over-limit configuration/action-slot operation and unsupported commands; Free exposes only approved one-tap actions; no limit affects payment receipt, durable ingestion, existing queues or alert delivery | Alerts API/store/routes, Web Companion editor, mobile `CompanionApi`, migrations `0041` and `0042`, OpenAPI layout path; disposable PostgreSQL harness proves Creator 32-slot persistence, operator save, moderator denial, 33-slot rejection and durable alert preservation; local pass. Cross-tier/device negative matrix remains pending device evidence |
| L07-05 | Connect each desktop helper to OBS WebSocket with invalid, rotated and revoked credentials | Local-only connection is authenticated, scoped, consented and revocable; credentials never appear in logs or support bundles | Windows/macOS security tests; not run |
| L07-05a | Give the native helpers loopback, non-loopback, wrong-port, query-credential and OBS v5 challenge/identify inputs | Only `ws(s)://localhost|127.0.0.1|::1:4455` without userinfo/query/fragment is accepted; OBS v5 challenge-response is computed correctly; malformed or unauthenticated hello/identify fails closed; no listener or raw frame logging is created | macOS `OBSWebSocketClient.swift` and `OBSWebSocketClientTests.swift`; Windows `OBSWebSocketClient.cs`; macOS `swift test` 12/12 pass. Windows SDK/runtime and real OBS evidence remain pending |
| L07-06 | Disconnect network, restart app/helper, expire a session and reconnect | Companion state recovers without altering accepted payments or dropping alert evidence | Offline/reconnect/crash matrix; not run |
| L07-07 | Test accessibility, supported OS floors, notification privacy, secure storage and release signing | Approved OS floors and accessibility requirements pass; store privacy declarations match actual data use | Device/store review evidence; not run |
| L07-08 | Bootstrap the React Native mobile repository and run local static/unit checks | The repository is pinned to React Native 0.87.0 with matching 20.2.0 CLI/tooling, contains iOS/Android projects without copied debug signing material, and Jest/lint/TypeScript/config checks pass | `bharatstudio-companion-mobile/package.json`, native scaffold; `npm test -- --runInBand` (current 23 tests), `npm run lint`, `npm run typecheck`, `npx react-native config`; pass locally on 2026-08-15 |
| L07-09 | Audit the bootstrapped mobile dependency graph | Release cannot proceed while known high-severity transitive advisories are unreviewed; forced dependency downgrades are not applied automatically | `npm audit --omit=dev --audit-level=high`; RN 0.87.0/CLI 20.2.0 upgrade leaves the audit metadata at eight high findings through the Metro/RN/image-size graph; upstream fix or risk acceptance remains pending |
| L07-09a | Install dependencies and verify the local Metro parser hardening | The postinstall patch is applied deterministically; malformed zero-length parser-box guards are present; a malformed zero-length ICNS fixture fails fast; the mitigation does not change the app API boundary | `patches/image-size+2.0.2.patch`, `tests/dependency-hardening.test.cjs`; clean `npm ci --ignore-scripts`, `npm run postinstall`, `npm run test:dependencies` (2/2), mobile Jest 23/23, lint and TypeScript; pass locally on 2026-08-15 |
| L07-10 | Render the signed-out Companion shell without a configured sign-in callback | The shell clearly shows an unconnected state, does not fabricate account/payment metrics, and states that OBS control requires a paired helper | `src/CompanionShell.tsx`, `__tests__/App.test.tsx`; `npm test -- --runInBand`, `npm run lint`, `npx tsc --noEmit`; pass |
| L07-11 | Call the Companion API client with no token, a valid token, a path containing reserved characters, an undersized idempotency key and a forbidden action response; read/save a versioned layout | Missing credentials fail before network access; HTTPS/bearer/path encoding, 16–128-character idempotency-key bounds, action allowlist, layout page-size/slot envelope, runtime response validation and error mapping follow the client contract; provider response bodies are not surfaced | `src/api/CompanionApi.ts`, `src/api/CompanionApi.test.ts`; mobile Jest 23/23, lint and TypeScript checks; pass locally after client/layout contract alignment |
| L07-14 | Render the signed-in mobile shell with server-provided channel/role/state and invoke its available queue actions | The shell renders only supplied state, shows pending/overlay status, exposes only the finite pause/resume action set, disables operations for non-operator roles, lets a multi-channel account select a server-authorised channel without assuming the first array item, filters stale state/queues to the selected authorised channel, and passes an explicitly selected active queue ID to each command; no payment or YouTube data is fabricated | `src/CompanionShell.tsx`, `src/api/CompanionApi.ts`, `__tests__/App.test.tsx`, `src/api/CompanionApi.test.ts`; 23 mobile tests, lint and TypeScript checks; pass locally |
| L07-12 | Build and test the macOS Companion shell and local native policy boundary | SwiftUI executable package builds at the provisional macOS 11 target; the unpaired shell contains no OBS/network authority; expired/revoked/wrong-session/wrong-channel commands fail closed; only finite confirmed actions are accepted; credential-like support-log values are redacted; approved REST response shapes decode with exact wire keys and unsupported actions are rejected | `bharatstudio-companion-desktop/macos/Sources/BharatStudioCompanionMacOS/CompanionPolicy.swift`, `CompanionContract.swift`, `CompanionPolicyTests.swift`, `CompanionContractTests.swift`; `swift test` 7/7, pass locally on 2026-08-15. Pairing, Keychain, OBS and device evidence remain pending |
| L07-13 | Build the Windows WinUI 3 helper | Windows project builds, signs and runs through the approved device/security matrix | `bharatstudio-companion-desktop/windows/BharatStudio.Companion.Windows.csproj`, XAML shell and fail-closed policy boundary now exist; Windows App SDK build agent, package assets, signing and device/security matrix not run |
| L07-15 | Acquire, renew, compete for and revoke Companion control sessions on a Free channel | Server role and current-user checks apply; the same client instance renews its existing lease; a second active client is rejected; revocation is retained and frees the channel; direct table writes are denied; no payment, queue, outbox or delivery row changes | `0053_v1_l07_companion_control_sessions.sql`, `packages/db/tests/l03_application_behavior.sql`; `pnpm db:test:l03`; local pass. Cross-device, native pairing and deployed evidence remain pending |
| L07-16 | Acquire and revoke a Companion control session through the authenticated API and mobile client | POST/DELETE use the versioned channel-scoped contract, bearer authentication, bounded client identity fields and server-owned lease response; DELETE accepts a `204` without attempting to parse a body; invalid client/session inputs fail before network access; wrong-channel revocation is rejected by the database function | Alerts `apps/api/src/routes/companion.ts`, `apps/api/src/db/alert-store.ts`, OpenAPI `v1.yaml`, mobile `src/api/CompanionApi.ts` and tests; API 54/54, build, OpenAPI 32 paths, mobile 23/23, lint/typecheck and `pnpm db:test:l03` pass locally on 2026-08-15. Native/device/deployed evidence remains pending |
| L07-17 | Stall or reject a mobile Companion API request, return malformed JSON, and exercise an out-of-range timeout configuration | Requests are aborted after the bounded client timeout, transport/decoding failures map to `request_failed` without exposing raw details, and unsafe timeout values are rejected before network access; no Alerts/payment/queue state is changed | `bharatstudio-companion-mobile/src/api/CompanionApi.ts`, `src/api/CompanionApi.test.ts`; 23 mobile tests, lint and TypeScript pass locally; device/offline/reconnect evidence remains pending |
| L07-18 | Decode valid and malformed Companion response envelopes on mobile | Valid v1 state/layout/control-session responses are accepted; UUID identifiers, bounded labels/pages, unique slot indexes and exact known fields are enforced; negative counts/dates, unknown fields and unsupported layout/actions are rejected before UI use and map to a bounded `request_failed` error | `bharatstudio-companion-mobile/src/api/CompanionApi.ts`, `src/api/CompanionApi.test.ts`; Jest 23/23, lint, TypeScript, dependency-hardening 2/2 and React Native config pass locally on 2026-08-15. Device/offline/reconnect, Windows and deployed evidence remain pending |
| L07-19 | Decode valid and malformed Web Companion response envelopes | Signed-in user, queue list, Companion state/layout and action-result payloads require the v1 envelope, valid UUID/date/count/slot values and approved tier/action sets; malformed data fails closed before UI use | `bharatstudio-alerts/apps/web/app/lib/api.ts`; Web TypeScript check and production build pass locally on 2026-08-15. Authenticated browser, deployed and cross-replica evidence remain pending |
| L07-20 | Decode Windows C# state/layout/control-session/action-result responses with missing/null fields, unsupported slot ladders, default timestamps or unsafe client-instance IDs | The Windows decoder returns a bounded contract failure rather than accepting defaults or throwing a null-reference exception; only the approved v1 enums, `8/16/32/64` max-slot ladder, timestamps and client-instance alphabet are accepted | `bharatstudio-companion-desktop/windows/CompanionContract.cs`; source correction applied 2026-08-15. Windows/.NET SDK is unavailable in this environment, so compile/runtime/device evidence is not run |
| L07-21 | Open Web Companion with valid server projections for history, plan, sessions and current state | The screen set renders recent activity, health/freshness, plan/slot locks, device/session inventory and recovery guidance; session revocation uses the documented server endpoint; notification preferences remain explicitly unavailable until a persistence contract exists; malformed projections fail closed | `bharatstudio-alerts/apps/web/app/companion/page.tsx`, `apps/web/app/lib/api.ts`; Web TypeScript and production build pass locally on 2026-08-15; authenticated browser/accessibility/deployed evidence remains pending |
| L07-22 | Render mobile Home, Activity, Health, Settings, Sessions and Help screens and route history, billing and session projections through `CompanionApi` | Screens expose only supplied server state, show an explicit unavailable state for unimplemented notification persistence, and surface server-side session revoke without changing alert/payment state; malformed projections are rejected before UI use | `bharatstudio-companion-mobile/src/CompanionShell.tsx`, `src/api/CompanionApi.ts`, tests; Jest 24/24, lint and TypeScript pass locally on 2026-08-15; native auth/storage/push/offline/device evidence remains pending |
| L07-23 | Exchange a bounded Google identity token through the documented mobile auth endpoint | The client sends only the identity token and device label, accepts only the opaque v1 session response with expiry and validated user projection, maps 401/503/invalid responses safely, and does not persist plaintext credentials | `bharatstudio-companion-mobile/src/api/CompanionApi.ts`, `src/api/CompanionApi.test.ts`; Jest 26/26, lint and TypeScript pass locally on 2026-08-15; native Google SDK, Keychain/Keystore and device evidence remains pending |
| L07-24 | Write, read, clear, corrupt and expire a mobile Companion session | Session material uses the versioned Keychain/Keystore service with `WHEN_UNLOCKED_THIS_DEVICE_ONLY`; malformed/expired material is cleared; invalid writes and unavailable secure storage fail closed; no AsyncStorage/plaintext/in-memory credential fallback is used | `bharatstudio-companion-mobile/src/auth/SecureSessionStore.ts`, `src/auth/SecureSessionStore.test.ts`, `package.json`; Jest 29/29, lint and TypeScript pass locally on 2026-08-15. Real iOS/Android device, lock/restart/backup/restore and release evidence remains pending |
| L07-25 | Sign in, restore, expire and sign out through the mobile session controller | Google exchange result is persisted before API use; secure-store failure leaves the process signed out; restore never fabricates account state; expired access is cleared before use; sign-out clears in-memory state and secure storage | `bharatstudio-companion-mobile/src/auth/CompanionSessionController.ts`, `src/auth/CompanionSessionController.test.ts`; Jest 34/34, lint and TypeScript pass locally on 2026-08-15. App bootstrap/native Google/device evidence remains pending |
| L07-26 | Start the mobile app with a stored session, rejected session, or injected Google credential | Startup uses secure restore then server `/v1/me`; rejected sessions are cleared; sign-in accepts credentials only from the platform adapter and persists before account state is exposed; absent production configuration remains signed out | `bharatstudio-companion-mobile/src/auth/CompanionRuntime.ts`, `src/auth/CompanionRuntime.test.ts`, `App.tsx`; Jest 37/37, lint and TypeScript pass locally on 2026-08-15. Native SDK/configuration, projection loading, device and release evidence remain pending |
| L07-27 | Exercise mobile transport timeout/5xx, 401, 403, conflict, retry exhaustion and online transition | Retryable failures use bounded 1s–60s backoff and then show offline; 401 requests re-authentication; 403/client/conflict failures do not retry; online transition has no local delivery side effect; no payment/alert/queue buffering is performed by the client | `bharatstudio-companion-mobile/src/recovery/CompanionRecoveryPolicy.ts`, `src/recovery/CompanionRecoveryPolicy.test.ts`; policy 4/4 and full Jest 41/41, lint and TypeScript pass locally on 2026-08-15. Native listeners, background integration and device evidence remain pending |
| L07-30 | Restore/sign in, load the selected authorised channel projection, then switch channels | Mobile fetches server-owned state, queues, history, billing and sessions only after authentication; malformed projections fail closed; latest-request protection prevents an older channel response overwriting the newest selection; no local entitlement/payment decision is made | `bharatstudio-companion-mobile/src/auth/CompanionRuntime.ts`, `App.tsx`, `src/auth/CompanionRuntime.test.ts`; projection test and full Jest 46/46, lint and TypeScript pass locally on 2026-08-15. Authenticated device, accessibility, offline and deployed evidence remain pending |
| L07-31 | Revoke a non-current mobile account session from the Sessions screen | The app calls the authenticated server revoke endpoint, refreshes the projection after success, keeps current-session revoke unavailable, and shows bounded recovery on failure; no payment/alert/queue state changes | `bharatstudio-companion-mobile/App.tsx`, `src/CompanionShell.tsx`, `src/api/CompanionApi.ts`; mobile Jest 46/46, lint and TypeScript pass locally on 2026-08-15. Authenticated browser/device and deployed evidence remain pending |
| L07-32 | Decode notification payloads and registration tokens; schedule background refresh | Only privacy-minimised operational notifications and bounded platform tokens are accepted; sensitive/unknown fields fail closed; background refresh interval remains bounded and has no financial/alert buffering side effect | `bharatstudio-companion-mobile/src/notifications/CompanionNotificationPolicy.ts`, test; policy 3/3 and full Jest 49/49, lint and TypeScript pass locally on 2026-08-15. APNs/FCM, server preferences, background hooks and device evidence remain pending |
| L07-33 | Request native permission, register/refresh APNs/FCM token, submit it to the authenticated API, update preferences, list devices and revoke one | The mobile adapter uses explicit provider registration, accepts only privacy-minimised messages, and sends a platform-labelled token to the server. The server validates the envelope, encrypts token material, stores only a fingerprint plus ciphertext, returns no token, and scopes preference/device operations to the authenticated user. Revoke disables the device without deleting audit evidence; no tip/payment data enters push payloads | Mobile `src/notifications/FirebaseNotificationAdapter.ts`, `src/api/CompanionApi.ts`; Alerts `apps/api/src/routes/me.ts`, `apps/api/src/notifications/token-crypto.ts`, migration `0057_v1_l07_notification_preferences_and_devices.sql`, `apps/api/test/notification-routes.test.ts`; Alerts 57/57, build; mobile 52/52, lint/typecheck. Physical-device/provider/background evidence remains pending |
| L07-28 | Acquire, renew, expire, conflict and revoke the mobile Companion control lease | Mobile uses client type `mobile` and a stable instance identity; same-session renewal is accepted; a replacement session fails closed; expiry disables local active state; revocation clears local state and calls the server; no payment/alert/queue row is changed | `bharatstudio-companion-mobile/src/control/CompanionControlSessionManager.ts`, `src/control/CompanionControlSessionManager.test.ts`; lifecycle 4/4 and full Jest 45/45, lint and TypeScript pass locally on 2026-08-15. UI wiring, cross-device/native/deployed evidence remains pending |
| L07-29 | Validate the macOS secure session store contract with short, expired, bounded and service-identifier cases | Keychain store uses the versioned non-synchronised service, rejects invalid/expired credentials, and has no plaintext/UserDefaults fallback; malformed/expired records are deleted by the implementation | `bharatstudio-companion-desktop/macos/Sources/BharatStudioCompanionMacOS/CompanionKeychain.swift`, `CompanionKeychainTests.swift`; `swift test` passes 10/10 locally on 2026-08-15. Real Keychain/sandbox/lock/restart/signing/pairing evidence remains pending |

## Release blockers

- React Native exact patch, iOS/Android floors, package identifiers, signing and store accounts must be recorded before store submission.
- Windows/macOS signing/notarisation/distribution evidence is required.
- The mobile dependency audit must have an approved remediation or documented risk acceptance before release.
- Companion must remain independently disableable without disabling Alerts delivery.
- APNs/FCM provider credentials, Apple/Google release configuration, physical-device permission/background delivery, and token-key rotation evidence must pass before push is enabled in production. Push remains operational best-effort; server/API projections remain authoritative.

### Fresh local audit evidence — 2026-08-15

The web/API, mobile, macOS, database and Windows-contract surfaces were
reviewed together after the local regression pass. No additional local failure
was found. The current local evidence is: mobile 24/24 tests,
lint/typecheck/config and dependency-hardening checks; macOS `swift test`; API
tests/build/OpenAPI; and the disposable PostgreSQL role/session harness.

Not satisfied by this review: authenticated device/store tests, real native
pairing and OBS WebSocket authentication, secure-keychain/DPAPI evidence,
Windows build/signing, macOS notarisation, offline/reconnect/crash matrix,
accessibility/device matrix, and dependency-audit remediation or risk
acceptance. These remain release gates rather than assumed pass conditions.

### Mobile transport-boundary evidence — 2026-08-15

The mobile client now has a bounded request timeout and safe transport/JSON
failure mapping. The default timeout is 10 seconds and accepted overrides are
limited to 1–60 seconds. Mobile tests pass 23/23, lint and the explicit
`npm run typecheck` script pass,
dependency hardening passes 2/2, React Native config resolves both native
projects, and macOS `swift test` remains green. Device offline/reconnect/crash
and native helper evidence remain pending.

### macOS native policy evidence — 2026-08-15

The macOS package now has five passing tests covering the existing unpaired
shell plus active-lease command acceptance, explicit-confirmation enforcement,
expiry/revocation, session/channel binding and credential-like support-log
redaction. The package entry point was renamed to `CompanionApp.swift` to
remove the Swift 6 `@main`/`main.swift` build conflict. This remains local
policy evidence only; it does not close native pairing, cryptographic signing,
Keychain, OBS WebSocket, device, notarisation or distribution gates.

### Windows project-boundary evidence — 2026-08-15

The Windows repository now contains the proposed WinUI 3/C#/XAML project
boundary, inactive/unpaired shell, least-privilege app manifest and a local
policy/redaction core. The shell has no OBS connection, public listener,
credential storage, arbitrary command path or support upload. This is source
and contract evidence only. The current macOS workspace has no Windows
SDK/.NET toolchain, so compilation, MSIX packaging, package assets, signing,
Credential Manager/DPAPI integration, pairing, OBS WebSocket authentication
and device/security evidence remain open.
