# Cross-repository acceptance records

Create one acceptance record per approved launch task, following the task ID: `TC-L00-...md` through `TC-L10-...md`.

Each case defines setup, synthetic test data, action, expected result, failure/retry behaviour, observability evidence, cleanup/rollback, and owning repository. Payment, database, security, privacy, and deployment cases require isolated environments and no real personal/payment data.
