# TC-RT-11 — query timeouts on the widget/dashboard/analytics read path

**Task:** `../active/tasks/RT-11.md`
**Owner:** Sukhdev Singh
**Status:** `Conditionally complete — local verification passed; external release evidence not applicable`

| Case | Control | Expected evidence |
|---|---|---|
| RT-11.1 | A pathological read fails fast and releases its connection | `RT-11.1/RT-11.3: runDerivedRead translates a 57014 cancellation into ReadTimeoutError and counts it exactly once`, `RT-11.1: a successful read within budget is returned unchanged, no translation`, `RT-11.1/RT-11.3: wrapWithReadTimeout translates a cancelled query on the wrapped Sql callable` (`rt11-read-timeout.test.ts`). Connection release itself is Postgres's own `statement_timeout` behaviour (the server cancels and returns the connection to the pool automatically — PostgreSQL documentation, review record §1), not something this application code has to separately implement; what this application layer owns is turning that cancellation into a clean typed error rather than an opaque one |
| RT-11.2 | The timeout never applies to a payment write, webhook commit or migration — enforced in code, and tested | `RT-11.2: the timeout wrapper never applies to anything but the call it wraps...` documents the structural boundary directly (`db/derived-read-pool.ts`'s pool is only ever constructed for and wired to five specifically-named read-only stores — `index.ts`); `RT-10.4`'s payment-order-write integration test (`tests/TC-RT-10-backpressure.md`) additionally proves the shared chokepoint (`classifyReadPriority`) returns `null` — no governance of any kind, timeout included — for every non-GET request, which covers every payment write and webhook commit this API ever issues. Migrations run through `packages/db/migrations/*.sql` via tooling entirely outside this Node process and never construct a `Sql` handle through `createDerivedReadSql` at all |
| RT-11.3 | A fired timeout is counted and returns a clear retryable error | `RT-11.1/RT-11.3` tests above (counted exactly once, never double-counted, never miscounted on a non-timeout error — see `RT-11: runDerivedRead never masks a non-timeout error as a timeout, and never counts it as one`). "Clear retryable error": verified structurally in `active/tasks/RT-11.md`'s Failure behaviour field — every one of the five call sites this timeout can reach already wraps ITS OWN store call in a generic `catch` that returns `503 …_unavailable, retryable: true` for any thrown error, so a `ReadTimeoutError` surfaces through the EXACT SAME response shape a creator/supporter already sees today for any other store failure (read directly from `routes/goals.ts`, `routes/challenges.ts`, `routes/interactions.ts`, `routes/payments.ts` before this task touched any of them) |
| RT-11.4 | A configured value below the path's §19.4 budget is rejected at startup | `RT-11.4: a configured statement timeout below §19.4's API-read p99 budget (200ms) is rejected at startup` and `RT-11.4: a value at or above the budget boundary is accepted` (`rt11-read-timeout.test.ts`) — `loadConfig` throws for `WIDGET_ANALYTICS_STATEMENT_TIMEOUT_MS=199`, accepts `200` and above |
| RT-11.5 | Unset configuration leaves behaviour exactly as it is today | `RT-11.5: unset configuration leaves the derived-read Sql handle identical to the main one — no wrapping, no pool, nothing observable changes` — asserts **reference equality** between the returned `Sql` handle and the exact `mainSql` instance passed in when both `WIDGET_ANALYTICS_POOL_MAX` and `WIDGET_ANALYTICS_STATEMENT_TIMEOUT_MS` are unset; nothing is constructed, nothing is wrapped |
| RT-11.SHARED | Statement-timeout error recognition is exact, not heuristic | `RT-11: isStatementTimeoutError recognises only Postgres 57014, never a different failure misreported as a timeout` — a `unique_violation` (`23505`) and a plain untagged `Error` are both proven NOT to be misclassified as a timeout |

**Commands (from `bharatstudio-alerts`):**
`pnpm --filter @bharatstudio/alerts-api build` · `pnpm --filter @bharatstudio/alerts-api test` · `pnpm --filter @bharatstudio/alerts-web test` · `pnpm contracts:validate` · `pnpm db:test:all` · `pnpm db:test:l03` · `pnpm measurement:test` · `(cd services/alert-worker-go && go build ./... && go test -race ./... && go vet ./...)` · `(cd services/payment-webhook-go && go build ./... && go test -race ./... && go vet ./...)` · `git diff --check`

**Commands (from `bharatstudio-requirements`):** `python3 tools/doc_consistency.py` · `python3 tools/traceability.py`

**Security/data boundary:** no new personal-data field. `bsa_derived_read_timeout_total` is a bare unlabelled counter.

**External evidence:** none claimed. **Unverified, stated plainly:** this task did not run a real `statement_timeout` cancellation against a live Postgres connection end to end (a genuine `pg_sleep()`-style pathological query actually hitting SQLSTATE `57014` through `createDerivedReadSql`'s pool). What IS verified: (1) `options.connection` is merged into the real Postgres `StartupMessage` by the installed `postgres@3.4.9` library — read directly from its source, `node_modules/.pnpm/postgres@3.4.9/.../src/index.js` and `src/connection.js`, not assumed from documentation; (2) PostgreSQL's own documentation states `statement_timeout` aborts a statement exceeding the configured duration and is settable per-session (review record §1, quoted verbatim with URL and access date); (3) the 57014→`ReadTimeoutError` translation logic is proven against a fake `Sql`-shaped callable that returns a real Postgres `57014` error shape. The gap between (1)+(2)+(3) and an actual live cancellation is real and unclosed — a genuine end-to-end proof needs a live database, which is reasonable follow-up but was not performed here. The 200ms floor is §19.4's own number, not independently re-derived or re-measured by this task.

## Recorded local evidence

- `pnpm --filter @bharatstudio/alerts-api build` — clean.
- `pnpm --filter @bharatstudio/alerts-api test` — **564 passed, 0 failed** (547 RT-06 baseline + 7 RT-10 + 10 RT-11; see `tests/TC-RT-10-backpressure.md` for the same run, not repeated).
- Remaining commands (`pnpm --filter @bharatstudio/alerts-web test`, `pnpm db:test:all`, `pnpm db:test:l03`, `pnpm measurement:test`, both Go services, `git diff --check`, `doc_consistency.py`, `traceability.py`) — run once for the whole slice; exact counts recorded in the shared review record's "Checks run for the whole slice" section to avoid tripling identical output across three test records.
