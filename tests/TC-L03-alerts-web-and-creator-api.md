# TC-L03 — Alerts web and Creator API acceptance

**Status:** `Execution in progress — foundation and bounded overlay/API evidence available; full acceptance pending`
**Task:** [`tasks/L03-alerts-web-and-creator-api.md`](../tasks/L03-alerts-web-and-creator-api.md)  
**Authority:** [`active/launch/00_LAUNCH_SCOPE_AUTHORITY.md`](../active/launch/00_LAUNCH_SCOPE_AUTHORITY.md)  
**Repository:** `bharatstudio-alerts`  
**Data:** synthetic users, channels, payments, alerts and overlay sessions only

This is the acceptance record for the Alerts web and TypeScript Creator API. It does not approve the draft API, database migrations, Razorpay production connection, or any YouTube/Enterprise capability.

## Preconditions

1. L01 contract verification is complete for the exercised endpoints and fixtures.
2. L02 isolated RLS/archive verification is complete, or the run is explicitly marked conditional with the missing evidence recorded.
3. A disposable staging database and provider sandbox are used; no production credentials or real donor data.
4. The test environment has separate synthetic identities for owner, admin, operator, moderator, viewer, unauthenticated donor and an unrelated channel member.
5. The web build is served over HTTPS with the approved origin, CSP, CORS and cookie/session settings.

## Acceptance cases

| ID | Setup and action | Expected result | Failure/retry and evidence |
|---|---|---|---|
| L03-01 | Sign in with Google authentication-only scopes; load current user and channels | Session is created, revoked device is rejected, no YouTube scope/request appears | Auth trace and provider scope capture; no token value logged |
| L03-02 | Owner creates/edits a channel profile and public tip presentation; unrelated user attempts the same | Owner-approved fields persist; unrelated user receives 403/404 without resource disclosure | API trace, authorization decision, DB/RLS evidence |
| L03-03 | Open public channel by handle with active, paused and missing states | Only approved public fields appear; paused/missing state is safe; no internal IDs/topology/secrets/audit details leak | Response snapshot and negative secret/topology scan |
| L03-04 | Submit valid, below-minimum, malformed, duplicate and rate-limited tip-order requests | Valid request is idempotent; invalid/minimum/rate-limit responses use documented error envelopes; browser never declares capture | Provider sandbox order evidence, idempotency replay, Retry-After evidence |
| L03-04i | Submit a public tip-order request with a malformed or unsafe `Idempotency-Key` header | The API returns a bounded 400 validation error before invoking the payment boundary; valid 16–128 character keys using `A-Za-z0-9._:-` remain accepted | `apps/api/src/routes/public.ts`, `apps/api/test/app.test.ts`; API route suite 40/40 and TypeScript check pass locally; provider/deployed/staging evidence pending |
| L03-04j | Submit a Companion action with a malformed or unsafe `Idempotency-Key` header | The API rejects the key before the durable command store is invoked; an absent key retains the established `idempotency_key_required` response | `apps/api/src/routes/companion.ts`, `apps/api/test/app.test.ts`; API suite 55/55 and OpenAPI validation pass locally; deployed/device evidence pending |
| L03-04b | Configure the public Razorpay key and submit a valid prepared order; repeat with the key absent | Configured checkout opens with the server-created provider order; absent configuration fails clearly; browser callback remains informational and signed webhook remains financial truth | `apps/web/app/tips/[handle]/TipForm.tsx`; web build pass; provider sandbox/staging evidence pending |
| L03-04d | Inspect the configured Razorpay Checkout timeout for a valid prepared order | Checkout receives the approved 900-second UI timeout, while financial state remains webhook/reconciliation-owned and the server/database local intent cap remains fifteen minutes | `apps/web/app/tips/[handle]/TipForm.tsx`; web build pass; provider sandbox/staging evidence pending |
| L03-04e | Feed the public tip form valid, malformed, mismatched and unexpected order/status responses | The browser uses only a bounded local-order/provider-order envelope and server-confirmed INR status; invalid IDs, amounts, currencies, timestamps or statuses fail closed without opening checkout or declaring payment success | `apps/web/app/tips/[handle]/tip-contract.ts`, `TipForm.tsx`, `tip-contract.test.ts`; focused contract tests 3/3, Web TypeScript check and production build pass locally; provider/browser/staging evidence remains pending |
| L03-04a | Submit a tip with on-stream alert consent disabled | The browser forwards the explicit choice; payment/order confirmation remains independent of alert projection and the UI does not claim an on-stream display | API/DB L04-24 evidence; provider checkout handoff remains staging-gated |
| L03-04c | Configure a channel minimum above ₹10 and submit a lower amount | The public projection and tip form show the channel minimum; the API rejects the request before payment-service use, and the database intent boundary independently rejects a bypass attempt | `apps/api/test/app.test.ts`, migration `0043_v1_l03_l04_channel_tip_minimum.sql`, `pnpm db:test:l03`; local pass |
| L03-05 | View payment pending/success/failure/refund states from creator and donor-safe surfaces | State is read from server/payment boundary; no client callback changes financial truth; historical gross amount and status remain accurate | Synthetic ledger reconciliation and trace IDs |
| L03-05a | Poll the public tip-order status after checkout, including paid, pending, expired/failed and unknown order states | The donor surface reads only the narrow server projection; provider IDs, donor fields and internal routing are absent; browser callback alone cannot mark payment successful | `GET /v1/public/tip-orders/{orderId}/status`, migration `0044_v1_l03_public_payment_status.sql`, `apps/api/test/app.test.ts`, `TipForm.tsx`; local API/web/SQL checks pass; provider/staging evidence pending |
| L03-06 | Owner/admin/operator creates, rotates, revokes and expires an overlay session; viewer/moderator attempts session management; attacker reuses token against another overlay | Only operational roles can manage the credential; viewer/moderator management attempts are rejected by RLS; only the scoped active session connects; revoked/expired/wrong-overlay token fails; token is present only in the deliberate copied OBS URL fragment and in-memory Authorization header, never in API request URLs, referrers, logs or analytics; an event received but not acknowledged is replayed after reconnect | Header-authenticated token lifecycle and role-negative tests, log redaction scan, acknowledged-cursor reconnect evidence, 401/410 evidence |
| L03-06a | Use the authenticated Overlay Setup page to create, copy, rotate and revoke a session | The page calls the versioned API, shows expiry, never displays a raw token outside the generated URL field, and informs the user after rotation/revocation | `apps/web/app/overlay/setup/page.tsx`, `apps/web/app/lib/api.ts`; web build pass; browser/staging evidence pending |
| L03-08a | Pause and resume a queue from the authenticated dashboard | The update is authorized server-side, the returned queue state is rendered, and a missing/cross-channel target cannot produce an accepted state change | `apps/web/app/dashboard/DashboardClient.tsx`, `apps/web/app/lib/api.ts`, `apps/api/src/db/alert-store.ts`; API/web build pass; browser/staging evidence pending |
| L03-08b | Create an overlay session for one channel, commit a delivery for another channel in the same database, then replay through two independent API/store replicas | The valid overlay token returns only its own channel's deliveries; a delivery committed through shared PostgreSQL is visible on replica B, acknowledgement through B is observed by replica A, and cross-channel rows are never returned | `packages/db/migrations/0054_v1_l03_overlay_event_channel_guard.sql`, `integration/overlay-cross-replica.integration.ts`; disposable PostgreSQL harness; pass locally; Neon/Cloud Run/OBS staging evidence pending |
| L03-10a | Use the dashboard moderation controls on a loaded history item | The action uses the documented authenticated route; server/RLS role and event/channel checks decide acceptance; no client-only status is fabricated | `apps/web/app/dashboard/DashboardClient.tsx`, `apps/web/app/lib/api.ts`; web build pass; browser/staging evidence pending |
| L03-10c | Load the dashboard as owner/operator/moderator/viewer | Queue/test/Companion controls and moderation actions are shown only for the relevant role, while direct unauthorized requests remain rejected by the server | `apps/web/app/dashboard/DashboardClient.tsx`, `apps/api/src/routes/alerts.ts`, `apps/api/src/routes/channels.ts`; web/API build pass; browser/staging evidence pending |
| L03-10b | Submit a manual/test alert with selected queues, then inspect the durable outbox | The event, outbox and one immutable ready delivery per selected queue commit atomically; no-active-queue submission fails before acceptance | `packages/db/migrations/0019_v1_l03_manual_alert_deliveries.sql`, `apps/api/src/db/alert-store.ts`, `packages/db/tests/l03_application_behavior.sql`; database harness and API typecheck; pass |
| L03-14 | Create a channel and open its public tip surface before any manual queue setup | The channel creation transaction includes one active `Main alerts` queue; existing active channels with no open queue receive one deterministic backfill queue; payment alert routing never starts with zero destinations solely because onboarding omitted queue setup | `packages/db/migrations/0025_v1_l03_default_alert_queue.sql`, `packages/db/tests/l03_application_behavior.sql`; `pnpm db:test:l03`; pass |
| L03-15 | Close a queue while a payment delivery is being resolved, then attempt a new delivery insert | Closed queues are excluded from payment binding resolution; the database rejects a race-created delivery for a closed queue; the final open queue cannot be closed | `0026_v1_l03_open_queue_delivery_guard.sql`, `services/payment-webhook-go/internal/ingress/sql_store.go`, `packages/db/tests/l03_application_behavior.sql`; `pnpm db:test:l03`; pass |
| L03-16 | Submit configuration and binding override objects above the provisional envelope cardinality | API rejects more than 128 top-level properties and enforces a 64 KiB request envelope; no values are persisted or passed to the store; approved core configuration keys are enforced by the reviewed schema | `apps/api/src/app.ts`, `apps/api/src/routes/channels.ts`, `apps/api/test/app.test.ts`; 53 API tests and build; pass |
| L03-16a | Update a routing binding, close/reopen a non-default binding, edit approved priority/presentation overrides, attempt to close or rewrite the reserved default binding, and create a binding for a closed queue | Future routing policy changes are accepted without changing accepted delivery snapshots; default payment route remains active; identity rewrite and closed-queue binding creation are rejected at the database boundary; arbitrary HTML/template fields are not exposed | `apps/api/src/routes/channels.ts`, `apps/api/src/db/channel-store.ts`, `apps/web/app/dashboard/DashboardClient.tsx`, migrations `0037_v1_l03_binding_identity_guard.sql` and `0038_v1_l03_open_binding_queue_guard.sql`, `packages/db/tests/l03_application_behavior.sql`; API test, `pnpm db:test:l03`, build; pass locally; staging/UI gate pending |
| L03-17 | Send malformed JSON and JSON beyond the 64 KiB API body limit | Malformed input returns a bounded 400 and oversized input returns a bounded 413; parser details, stack traces and payload contents are not exposed | `apps/api/src/app.ts`, `apps/api/test/app.test.ts`; 53 API tests and API/web build; pass |
| L03-18 | Send health requests with the configured and an unapproved `Origin` | Helmet security headers remain present; credentialed CORS is returned only for the configured app origin and is not reflected for arbitrary origins | `apps/api/src/app.ts`, `apps/api/test/app.test.ts`; 53 API tests and API/web build; pass |
| L03-19 | Construct authenticated web requests with channel, queue, alert and overlay identifiers and invoke a Companion action | Client encodes every path identifier and only emits the reviewed Companion action union; idempotency requires browser secure randomness; server-side schema/RBAC remains authoritative | `apps/web/app/lib/api.ts`, `apps/web/app/dashboard/DashboardClient.tsx`; API/web production build; pass |
| L03-20 | Request an unknown API route | The versioned 404 envelope contains the request trace identifier, with no internal route details | `apps/api/src/app.ts`, `apps/api/test/app.test.ts`; API test suite; pass |
| L03-21 | Open overlay setup for a selected channel when the account has multiple authorized channels | The UI accepts only a channel ID present in the authenticated `/v1/me` result, defaults safely when absent/invalid, and sends the selected ID to the existing server-authorized session route | `apps/web/app/dashboard/DashboardClient.tsx`, `apps/web/app/overlay/setup/page.tsx`; API/web production build; pass |
| L03-22 | Page through alert history where multiple events have the same `created_at` timestamp | The response uses a deterministic `createdAt|eventId` cursor; the next page contains every same-timestamp event exactly once, ordered by event ID, while timestamp-only legacy cursors remain readable | `apps/api/src/db/history-cursor.ts`, `apps/api/src/db/alert-store.ts`, `packages/db/migrations/0027_v1_l03_history_cursor_tiebreak.sql`, `packages/db/tests/l03_application_behavior.sql`; 53 API tests and disposable PostgreSQL harness; pass |
| L03-23 | Submit approved core alert configuration, an unknown field and non-contiguous amount brackets | Valid core fields are accepted; unknown fields are rejected rather than silently stripped; bracket gaps/overlaps/final-bracket errors return explicit configuration errors before the store is called | `contracts/json-schema/channel-config.schema.json`, `apps/api/src/domain/channel-config-schema.ts`, `apps/api/src/routes/channels.ts`, `apps/api/test/app.test.ts`; 53 API tests and contract validation; pass |
| L03-24 | Load the authenticated dashboard configuration editor, change an approved field, save with the current version, then submit a stale version | The editor renders only approved core fields, sends `if-match-version`, refreshes the returned version after success, and surfaces a conflict without overwriting newer state | `apps/web/app/dashboard/DashboardClient.tsx`, `apps/web/app/lib/api.ts`, `apps/web/app/styles.css`; web production build; local pass, browser/accessibility evidence pending |
| L03-25 | Change the approved style, anchor, scale, locale and reduced-motion settings in the dashboard preview | The preview updates without executing user content, remains bounded within its stage, shows safe multiline sample text and disables motion when selected; branding/template/HTML inputs are not exposed | `apps/web/app/dashboard/DashboardClient.tsx`, `apps/web/app/styles.css`; web production build; local pass, browser/accessibility evidence pending |
| L03-26 | Configure amount brackets, per-bracket display/TTS rules, TTS fallback, placement offsets, width, aggregation, rate limiting, approval and quiet mode | The editor emits only approved schema fields, preserves open-ended contiguous bracket semantics, omits an empty optional voice ID, shows bounded validation errors, and does not imply that presentation limits delete durable alerts | `apps/web/app/dashboard/ChannelConfigEditor.tsx`, `apps/web/app/lib/api.ts`, `apps/api/src/domain/channel-config-schema.ts`; `pnpm build`, `pnpm test`, `pnpm contracts:validate`, 53 API tests; local pass, browser/accessibility evidence pending |
| L03-27 | Deliver a ready overlay event with a versioned configuration, then exercise FIFO, priority, stacked, pills, aggregated, bracket timing, long-message, placement, scale, reduced-motion and TTS-failure settings | The browser uses the immutable accepted config snapshot, ignores unknown override keys, applies bounded display policy without HTML execution or clipping, supports each approved presentation mode, uses approved same-origin audio only when available and otherwise falls back to a non-blocking chime, and acknowledges every item only after its displayed group is confirmed in arrival order; a failed acknowledgement leaves the durable item replayable | `apps/web/app/overlay/overlay-policy.ts`, `apps/web/app/overlay/[overlayId]/page.tsx`, `apps/web/app/styles.css`, `packages/db/migrations/0029_v1_l05_overlay_config_snapshot.sql`; policy tests 6/6, web build, `pnpm db:test:l03`; local pass, provider/audio/browser/OBS and cross-replica evidence pending |
| L03-27a | Force a displayed group's cursor acknowledgement to return a non-retryable error after an earlier item in the group was acknowledged | The acknowledged prefix is never requeued; the unacknowledged suffix returns to the front of the durable client queue, the active SSE stream is aborted, and reconnect resumes from the last server-confirmed cursor without a stuck `active` state or dropped alert | `apps/web/app/overlay/overlay-policy.ts`, `apps/web/app/overlay/[overlayId]/page.tsx`, `apps/web/app/overlay/overlay-policy.test.ts`; focused overlay/TTS suite 10/10 and production build; local pass, deployed/browser/OBS evidence pending |
| L03-27b | Publish/acknowledge a newer delivery before an older delivery is acknowledged, then reconnect with the newer cursor | The older `ready`/`held`/`displayed` delivery is returned again despite being before the supplied cursor; only an acknowledged delivery is excluded, so concurrent task publication cannot cause permanent replay loss | migration `0055_v1_l03_unacknowledged_replay_guard.sql`, `l03_application_behavior.sql`; disposable PostgreSQL harness; local pass, deployed cross-replica/OBS evidence pending |
| L03-27c | Feed the browser overlay valid, wrong-version, malformed and unsupported SSE event envelopes | Only the approved v1 event shape, UUID/date/trace fields and event types reach presentation; invalid data is ignored without acknowledgement, so durable replay remains available | `apps/web/app/overlay/overlay-policy.ts`, `apps/web/app/overlay/[overlayId]/page.tsx`, `apps/web/app/overlay/overlay-policy.test.ts`; focused overlay/TTS suite 11/11, Web TypeScript check and production build pass locally; deployed/browser/OBS evidence remains pending |
| L03-27d | Inspect the overlay connection indicator with connected and reconnecting states | The indicator exposes an explicit accessible status role and polite state label while remaining visually unobtrusive in the OBS browser source | `apps/web/app/overlay/[overlayId]/page.tsx`; source review and web build; local pass, browser/assistive-technology/OBS evidence remains pending |
| L03-28 | Submit concurrent queue-creation and configuration-update operations for one channel | Queue entitlement checks are serialized by a channel-row lock before count evaluation, and configuration version allocation is serialized before reading the current version; concurrent callers cannot exceed the server-owned queue allocation or race the `(channel_id, version)` key | `apps/api/src/db/channel-store.ts`, `apps/api/test/channel-store-serialization.test.ts`, `integration/channel-store-concurrency.integration.ts`; API suite 54/54 and build; disposable PostgreSQL multi-connection integration passes with exactly one accepted queue create and one accepted next config version; staging/cross-replica capacity evidence remains pending |
| L03-07 | Connect overlay, receive events, disconnect, commit events through another replica, reconnect with `Last-Event-ID`, and fail cursor acknowledgement temporarily | Durable cursor/outbox replay returns eligible events in order without silent loss or incorrect duplicate effect; the browser does not advance its display queue until acknowledgement succeeds; notification outage does not block recovery | Cross-replica, notification-outage and acknowledgement-retry evidence; shared L05/L09 gate |
| L03-07a | Start staging/production configuration with the same app and direct database endpoint | Startup fails closed instead of silently putting LISTEN/NOTIFY on the pooled endpoint | `apps/api/src/config.ts`, API test; pass locally |
| L03-07b | Call readiness without configured dependencies, with a successful probe and with a failing probe | Unconfigured/failed probes return bounded `503`; only a successful configured probe returns `200 {status:"ready"}` | `apps/api/test/app.test.ts`, `apps/api/src/db/readiness.ts`; `pnpm test` and build; pass |
| L03-07c | Fail the direct overlay listener, allow reconnect, emit health metrics, then close the service | Listener reconnects with bounded backoff, health counters are exposed only through authenticated metrics, shutdown stops reconnect scheduling, and durable replay remains available | `apps/api/test/overlay-wakeup.test.ts`, `apps/api/src/db/overlay-wakeup.ts`, `apps/api/src/app.ts`; 53 API tests and build; pass locally; live database reconnect evidence pending |
| L03-07c-e | Let an already-open SSE stream encounter a transient durable replay failure | The route does not leak an exception or fabricate an HTTP success/error after headers are committed; it emits a bounded comment and closes cleanly so the browser reconnects with its last acknowledged cursor; no delivery is acknowledged or discarded | `apps/api/src/routes/overlay.ts`, `apps/api/test/app.test.ts`; API suite 54/54 and build; local pass; deployed replay/fault-injection evidence pending |
| L03-07c-int | Connect two independent direct PostgreSQL listeners, publish one committed `pg_notify`, and wake both overlay waiters | Both listener connections receive the same wake-up through the approved direct endpoint shape; successful `listen()` registration remains connected; notification remains best-effort and durable cursor replay remains authoritative | `integration/overlay-wakeup.integration.ts`, `apps/api/src/db/overlay-wakeup.ts`, `packages/db/tests/run-l03-application-behavior.sh`; disposable PostgreSQL 16 harness; local two-listener pass; Neon direct endpoint and cross-replica staging evidence pending |
| L03-07d | Submit a moderation action using an event from another channel while the actor operates the requested channel | RLS rejects the write; an action cannot attach an event to the wrong channel | `packages/db/migrations/0017_v1_l03_moderation_event_scope.sql`, `packages/db/tests/l03_application_behavior.sql`; `pnpm db:test:l03`; pass |
| L03-07e | Create a queue binding whose queue belongs to another channel | RLS rejects the write; a binding cannot route another channel's queue | `packages/db/migrations/0018_v1_l03_binding_queue_scope.sql`, `packages/db/tests/l03_application_behavior.sql`; `pnpm db:test:l03`; pass |
| L03-07f | Build the browser app without an API origin, or provide an invalid/local/non-HTTPS origin in staging/production | Development retains the local default; production fails closed for missing, invalid, localhost or non-HTTPS configuration; valid HTTPS origin is normalized without trailing slashes | `apps/web/app/lib/api-origin.ts`, `apps/web/app/lib/api-origin.test.ts`; focused tests 3/3 and web production build; pass locally |
| L03-08 | Configure two permitted queues for one source event; block queue A and leave queue B ready | Queue B progresses independently; source/priority and per-source override snapshots are projected to each delivery before rendering; no global status blocks B | Per-queue ledger/outbox/attempt/audit and overlay-projection evidence; `pnpm db:test:l03` covers the two-queue snapshot |
| L03-09 | Configure the approved template-library presentation together with alert text, branding, language, display size, multi-line content and reduced motion; preview long and Indic text | The approved catalogue/entitlement contract is enforced; preview matches configured size/placement; text is wrapped/truncated/continued by approved policy; no overlap, clipping or unsafe HTML; reduced motion remains usable | `active/launch/04_TEMPLATE_LIBRARY_AUTHORITY.md`, `contracts/template-catalogue.json`, entitlement mapping, screenshot/video matrix, locale list and DOM/CSS safety scan; contract is present, BSA-037 metadata is corrected, BSA-222/BSA-223 have local lifecycle evidence, but 359 runtime packages and visual/browser evidence remain pending. Additional package generation is intentionally deferred to the final implementation pass and is tracked in `pending/launch/PENDING-04-TEMPLATE-RUNTIME-PACKAGE-COMPLETION.md` |
| L03-10 | Owner/admin/operator/moderator/viewer attempt each dashboard, queue, moderation, history, billing and test-alert action | Owner/admin may view financial amounts; operator/moderator may operate or moderate and view alert content without financial amounts; viewer receives status-only history and no raw payment/refund rows; only permitted roles perform mutations; sensitive actions are audited. Billing displays the server-authoritative tier, price, annual/monthly mode, renewal state, auto-renew and grandfathering/grace metadata without client-side inference | Role matrix output and audit-event evidence; `0048_v1_l04_subscription_billing_projection.sql`, API billing projection and web client type/display are locally implemented; role/staging evidence remains pending |
| L03-11 | Web Companion Console requests an allowed operational state/action; browser attempts direct OBS/local API and disallowed command | Console uses documented server contract; no direct OBS connection or arbitrary local command is exposed; acknowledgement is traceable | Network capture, negative local-port test, command audit |
| L03-12 | Send malformed JSON, oversized fields, unsupported enum/version, invalid cursor, origin/CSRF/SSRF probes and burst traffic | Requests fail closed with bounded error envelopes; no stack traces/secrets/internal topology; retryable failures remain retryable and accepted evidence is not dropped | `apps/api/test/app.test.ts` proves the global 120-request/minute limiter returns bounded 429/`Retry-After` for a synthetic burst; remaining security probes, redacted logs and deployed rate-limit dashboard remain staging evidence |
| L03-13 | Deploy feature flag off, roll back web/API revision, replay synthetic pending state | Surface is disabled safely, prior compatible revision serves, no two writers process the same payment/queue state | Deployment/rollback record and reconciliation output |

### Overlay wake-up waiter lifecycle — 2026-08-15

`apps/api/src/db/overlay-wakeup.ts` now cancels a waiter timeout immediately
when notification or shutdown resolves it, preventing avoidable timer
accumulation during concurrent overlay activity. The API/overlay wake-up suite
is included in the 54/54 API pass and the API/web production build passes.
This is local resource lifecycle evidence only; direct-Neon, cross-replica,
fault-injection and OBS staging evidence remain pending.

## Local implementation evidence update — 2026-08-15

- API route suite now passes 54/54, including direct-listener reconnect/health behavior, multi-overlay wake-up broadcast, malformed-notification safety, bounded SSE event arrival after initial replay, clean closure after a post-headers replay failure, `Last-Event-ID` resume behavior, replica-B replay after a replica-A commit, no-wakeup durable polling replay, disconnected-waiter cancellation, fragment-only overlay session URL generation, public order boundary fail-closed behaviour, private Google-OIDC payment-client response validation, authenticated subscription-route input forwarding, strict subscription response validation, OIDC-protected maintenance route shell, configured readiness-probe behaviour, strict core configuration schema/semantic validation, configuration-envelope bounds, parser-error responses, exact-origin credentialed CORS, global burst-rate-limit handling, binding update behavior, composite history-cursor validation, Companion action target checks, safe entitlement-limit parsing, channel-specific public tip minimum enforcement and donor-safe payment status polling.
- API TypeScript build and web production build pass.
- Browser API-origin focused tests pass 3/3: development retains the local default, while missing/invalid/local/non-HTTPS staging or production origins fail closed; valid HTTPS origins are normalized.
- Disposable PostgreSQL harness passes the L02 remediation assertions plus L03/L04/L05 application behaviour.
- Disposable PostgreSQL harness proves two alert events sharing the same timestamp are ordered and paginated with the event-ID tie-breaker without loss or repetition.
- Disposable PostgreSQL harness proves role-scoped reads: owner/admin receive financial history fields, operator/moderator receive alert content without financial amounts, viewer receives status-only history, and only owner/admin can read raw payment rows through `bsa_app`.
- Disposable PostgreSQL harness also proves overlay-session management is limited to owner/admin/operator and Companion command insertion is limited to owner/admin/operator; viewer and moderator attempts are rejected by RLS. The overlay token data path remains separate from ordinary member reads.
- The same harness covers accepted multi-queue routing snapshots and idempotent private maintenance-run acceptance; these are implementation evidence, not cross-replica/staging proof.
- The same harness proves binding-backed duplicate consent rejection, mixed-consent protection, and that explicit multi-queue manual/test selection remains accepted without a source-binding row.
- The same harness proves a newly created/unseen payment source resolves through the reserved channel-default payment binding, while an exact source binding takes precedence. This is local database evidence only; provider and staging checkout evidence remains pending.
- Browser-source contract: the generated session URL resolves to an HTML overlay page, not the raw SSE endpoint; the page connects to the API stream, handles custom alert event names, displays queued ready/held alerts, preserves multiline text without clipping, and acknowledges a cursor only after the display interval. Verify in browser/OBS staging with a real session.
- Local browser smoke on 2026-08-15 loaded the web home route, public tip route, overlay route, overlay setup route and Companion route without a credential. The overlay remained in the safe `Overlay reconnecting` state, rendered no fabricated alert or provider error, exposed an accessible connection-status label, and produced no browser warning/error entries. The public tip page showed the bounded closed-tip state and ₹10 copy without checkout activation; overlay setup disabled session creation without an authenticated channel; Companion rendered its unauthenticated state, fabricated no channel/payment/queue data, and exposed no direct OBS/localhost command path. This does not satisfy authenticated event rendering, accessibility matrix, long-text/Indic, reduced-motion, acknowledgement-retry, cross-replica or OBS staging evidence.
- The overlay connection indicator now uses an explicit `role="status"` and polite live label, so its connected/reconnecting state is exposed by assistive technology instead of relying on an `aria-label` attached to a generic element. This is a local markup correction only; the authenticated browser/OBS accessibility matrix remains pending.
- Overlay security: a user from channel B cannot revoke or rotate channel A's overlay by supplying its overlay ID; the database transaction must require channel membership for both operations.
- Full acceptance cases L03-01 through L03-13 remain staging/application evidence, not inferred from these local tests.

### Overlay disconnect cancellation — 2026-08-15

The overlay route now aborts the wake-up waiter and polling fallback when the
raw SSE request closes. The waiter removes its timeout and abort listener
immediately; durable replay and acknowledgement semantics are unchanged. This
is covered by the 54/54 API pass and production build. Direct-Neon,
cross-replica, fault-injection and OBS staging evidence remain pending.

## Closure rules

- All applicable cases pass with dated evidence, or each exception is explicitly approved as `Conditionally complete` with owner and release gate.
- L03 cannot be marked `Verified` while L31/L32, overlay reconnect/resync, L02 RLS, or L04 payment-boundary prerequisites remain unproven.
- No accepted payment or alert may be deleted or silently discarded because a plan, queue, display, session or client is unavailable.

## Cleanup and rollback

Destroy synthetic staging records using the approved archival/retention procedure. Revoke test sessions and provider sandbox credentials. Roll back the feature flag/revision and reconcile the synthetic payment, outbox, delivery and overlay ledgers before closing the run.

## Response-boundary hardening cases — 2026-08-15

| ID | Setup and action | Expected result | Evidence and disposition |
|---|---|---|---|
| L03-04f | Feed the public tip page a valid, malformed, unknown-field, below-floor or out-of-range channel projection | Only the approved public fields are rendered; invalid or expanded projections fail closed to the safe closed-tip state; no internal field is rendered | `apps/web/app/lib/public-channel-contract.ts`, `apps/web/app/lib/public-channel-contract.test.ts`, `apps/web/app/tips/[handle]/page.tsx`; focused test passes; local pass, deployed/provider/staging evidence pending |
| L03-06b | Feed the authenticated setup surface a valid, malformed, query-credential, wrong-path or non-HTTP(S) overlay-session response | Valid sessions are accepted; malformed sessions are rejected; the UI never presents a query credential or arbitrary redirect URL, and decoder failures become bounded user-safe errors | `apps/web/app/lib/api.ts`, `apps/web/app/lib/api-contract.test.ts`, `apps/web/app/overlay/setup/page.tsx`; focused tests pass; local pass, authenticated browser/OBS/staging evidence pending |
| L03-10d | Feed the authenticated dashboard valid and malformed configuration, queue, binding, history, moderation, billing, entitlement and test-alert responses | Only bounded v1 values enter dashboard state; invalid UUIDs, enums, dates, ranges, unknown override keys and malformed financial/plan fields fail closed; optional history fields are normalized to `null` | `apps/web/app/lib/api.ts`, `apps/web/app/lib/api-contract.test.ts`; focused contract suite 19/19 and Web TypeScript check pass; local pass, authenticated browser/staging evidence pending |
| L03-10e | Feed the Web Companion layout update path a valid layout, duplicate slot indexes or a malformed server envelope | The mutation response is decoded with the same bounded layout contract as the read path; duplicate indexes, invalid tier/page/slot values and malformed envelopes fail closed; an unexpected body on the 204 revoke path is not accepted | `apps/web/app/lib/api.ts`, `apps/web/app/lib/api-contract.test.ts`; focused contract tests 4/4 and Web TypeScript/production build pass; local pass, authenticated browser/staging evidence pending |
| L03-10f | Feed the Web Companion action client valid and malformed accepted/rejected results | The complete command result envelope is validated, including command/event identity and timestamp; unknown fields, invalid status or malformed IDs/dates fail closed before UI use | `apps/web/app/lib/api.ts`, `apps/web/app/lib/api-contract.test.ts`; focused Web contract tests 5/5 and Web TypeScript/production build pass; local pass, authenticated browser/staging evidence pending |
| L03-10g | Feed authenticated identity, channel, queue, Companion-state/layout, binding, billing and entitlement responses with extra fields, invalid UUIDs, invalid roles or mismatched source values | Strict response envelopes and nested projections fail closed; only server-authorized v1 fields enter Web state, and the entitlement source is retained rather than silently dropped | `apps/web/app/lib/api.ts`, `apps/web/app/lib/api-contract.test.ts`; focused Web contract suite 19/19 and Web TypeScript/production build pass; local pass, authenticated browser/staging evidence pending |
| L03-04g | Feed the public tip client incomplete, expanded, amount-mismatched, wrong-provider or internal-provider-state order/status responses | The donor client requires the complete v1 OpenAPI response, exact requested amount, Razorpay provider and donor-safe status vocabulary; malformed or expanded responses cannot open checkout or claim payment success | `apps/web/app/tips/[handle]/tip-contract.ts`, `apps/web/app/tips/[handle]/tip-contract.test.ts`, `apps/web/app/tips/[handle]/TipForm.tsx`; focused Web/API contract suite 19/19 and Web TypeScript/production build pass; local pass, provider/browser/staging evidence pending |

### L03-04h — bounded Razorpay browser-asset handoff

**Result:** PASS (local browser-boundary evidence, 2026-08-15)

`razorpay-loader.ts` reuses an existing exact-origin Checkout asset, rejects a
marked script from another origin, fails a stalled load within the bounded
timeout, and resolves only after the expected asset exposes the constructor.
The loader has no payment-state authority and does not bypass the webhook or
ledger. Focused loader tests pass 5/5 and the Web production build passes.
Provider CSP, real browser, checkout, network-failure and staging evidence
remain open.

### L03-04k — bounded public order request and retry idempotency

**Result:** PASS (local browser-boundary evidence, 2026-08-15)

The public tip-order and status requests abort after bounded timeouts, forward
the caller’s idempotency key unchanged, reuse an existing key for retryable or
ambiguous responses, and retain it until the browser checkout handoff
succeeds. Focused `tip-client` tests pass 4/4 and the Web production build
passes. This does not prove deployed network behavior, provider retry,
Razorpay checkout or staging reconciliation.
