# RT-01 — idle overlay replay without polling

**Authority:** [`../../FULL-PRODUCT-DEFINITION.md`](../../FULL-PRODUCT-DEFINITION.md) §19.4 and §31.18.0
**Status:** `Conditionally complete — local implementation and verification; independent review unavailable`

| Field | Value |
|---|---|
| **Scope phase** | `v1`; overlay SSE idle/reconnect delivery only |
| **Owner** | **Sukhdev Singh** |
| **Tier and gate** | All tiers; free streaming remains account-free; no release claim |
| **Personal-data class** | Existing bearer/session and channel-scoped event metadata; no new fields |
| **Provider or legal dependency** | PostgreSQL LISTEN/NOTIFY and existing overlay session policy; no external provider |
| **Failure behaviour** | Durable replay remains authoritative; listener failure uses bounded jittered fallback; never acknowledge/drop an event |
| **Kill switch** | Disable listener optimization and close overlay streams via existing route controls |
| **Acceptance test** | `tests/TC-RT-01-overlay-idle-replay.md` |
| **Evidence location** | `tests/TC-RT-01-overlay-idle-replay.md`, API tests, and review |
| **Rollback** | Revert RT-01 route/wakeup changes; preserve existing migrations and cursor semantics |

## Boundaries

Initial replay remains mandatory. A connected idle stream must issue zero database replay queries during its window; one valid notification wakes one bounded durable replay. Only listener failure/disconnect may enter injected bounded jitter fallback. RT-02 channel fanout is excluded.
