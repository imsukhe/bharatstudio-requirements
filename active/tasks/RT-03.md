# RT-03 — checks, then synthesis, then one release

**Authority:** [`../../FULL-PRODUCT-DEFINITION.md`](../../FULL-PRODUCT-DEFINITION.md) §19.0 RT-03, §12.2, §19.4, §11.10, §11.11, §12.7, §31.18.0, §31.0, §35.1, §37.2, §37.11
**Status:** `Conditionally complete — local implementation and verification; independent review unavailable`

| Field | Value |
|---|---|
| **Scope phase** | `v1`; Phase 0.5 runtime remediation; alert-worker TTS enrichment path (checks → synthesis → release), TTS quota reservation/release, and overlay display-ordering only |
| **Owner** | **Sukhdev Singh** |
| **Tier and gate** | All tiers with TTS entitlement (Pro/Creator/Studio, §31.4 TTS-02); no release claim; blocks Phase 1 per §19.0 |
| **Personal-data class** | None new. No new personal-data field, no new retention rule. Alert message text and channel/event identifiers already flow through this path (0081, 0096); this task adds no new field carrying them, only a bounded classification token (`success` / `timeout_ambiguous` / `terminal`) logged via the existing `Logger.Event`/`logSafeError` shapes |
| **Provider or legal dependency** | None. Sarvam TTS synthesis (existing, `apps/api/src/tts/provider.ts`) and the existing internal worker→API HTTP call (`services/alert-worker-go/internal/tts/client.go`) only |
| **Failure behaviour** | The alert is always released and displayed; only whether a voice speaks varies. Checks (moderation, safety, quota, §11.10 routing) complete before synthesis begins, never after, never in parallel. Synthesis is held for at most one attempt, capped by the existing 2.5s HTTP client timeout in `internal/tts/client.go` — no new timeout, no cumulative budget. Only an unambiguous failure (connection refused, DNS, TLS, a 5xx, or a 429 with `Retry-After`) is retried, and only once; a timeout or context deadline is never retried (ambiguous — synthesis may already have been billed); a terminal failure (quota exhausted, safety rejection, unsupported voice/language, other 4xx) is never retried. Every failure class still reaches `Store.Release` next, unconditionally — `ObserveTaskOutcome` never sees `retryable` because of a synthesis failure, so no failure class can delay, duplicate or drop the delivery. A muted queue (`alert_queues.tts_muted_at`) never invokes synthesis (unchanged L07 behaviour, verified still true). A failed synthesis of every class leaves the creator's premium-character balance unchanged — this is `RT-03.6`, a blocking acceptance test enforced by `app_private.release_tts_usage_reservation` (migration `0128`), called from every synthesis-failure path in `apps/api/src/routes/tts.ts` |
| **Kill switch** | TTS enrichment as a whole is disabled by not wiring `handler.Config.Enricher` in `cmd/alert-worker/main.go` (requires `ALERT_WORKER_TTS_URL`/`ALERT_WORKER_TTS_AUDIENCE`; the worker fails closed at startup — `requiredEnv` — rather than silently running without TTS, matching the existing pre-task shape, unchanged by this task). With it unset, every delivery releases visual-only, exactly as a chime/`ttsAudioUrl: null` delivery already renders today. Quota metering is independently disabled per channel by tier (`ttsEnabled` entitlement dimension, quota `0` for `free` — unchanged) and per queue by the existing L07 mute switch (`alert_queues.tts_muted_at`), both of which this task preserves without weakening |
| **Acceptance test** | `tests/TC-RT-03-checks-synthesis-release.md` |
| **Evidence location** | `tests/TC-RT-03-checks-synthesis-release.md`, `services/alert-worker-go/internal/tts/client_test.go`, `services/alert-worker-go/internal/handler/cloud_tasks_test.go`, `apps/api/test/tts-quota-metering.test.ts`, `apps/api/test/l07-mute-synthesis-cost-gate.test.ts`, `apps/api/test/tts-fallback-reason.test.ts`, `apps/web/app/overlay/overlay-policy.test.ts`, `packages/db/tests/rt03_tts_quota_reservation_release.sql`, and this task's review record |
| **Rollback** | Revert `services/alert-worker-go/internal/tts/client.go`, `services/alert-worker-go/internal/tts/client_test.go`, `services/alert-worker-go/internal/handler/cloud_tasks.go`, `services/alert-worker-go/internal/handler/cloud_tasks_test.go`, `apps/api/src/domain/tts-quota.ts`, `apps/api/src/db/tts-quota-store.ts`, `apps/api/src/routes/tts.ts`, `apps/api/test/tts-quota-metering.test.ts`, `apps/api/test/l07-mute-synthesis-cost-gate.test.ts`, `apps/api/test/tts-fallback-reason.test.ts`, `apps/web/app/overlay/overlay-policy.ts`, `apps/web/app/overlay/overlay-policy.test.ts`, `packages/db/tests/run-l03-application-behavior.sh` (drop the added `rt03_tts_quota_reservation_release.sql` line), and delete `packages/db/tests/rt03_tts_quota_reservation_release.sql` (a test file, safe to delete directly, unlike a migration). Migration `packages/db/migrations/0128_v1_rt03_tts_quota_reservation_release.sql` is rolled back by a **new forward migration** that drops `app_private.release_tts_usage_reservation` — never by editing or deleting `0128` |

## Boundaries

**In scope:** failure classification and a bounded, single retry policy for the worker→API TTS enrichment HTTP call (`services/alert-worker-go/internal/tts/client.go`: `EnrichClass`/`EnrichOutcome`, `classifyTransportError`, `classifyStatus`); the `handler.Enricher` interface changing from `Enrich(...) error` to `Enrich(...) tts.EnrichOutcome`, and every implementation/call site updated (`cloud_tasks.go`, `cmd/alert-worker/main.go` — no code change needed there since `tts.Client` already satisfies the new interface structurally, verified by `go build ./...`); a new `Logger.Event("tts_enrich", outcome.Class, traceId)` call that never influences `Store.Release`/`Store.Retry`/`ObserveTaskOutcome`; the TTS premium-character reserve/release pair (`apps/api/src/domain/tts-quota.ts`'s `TtsQuotaMeter.release`, `apps/api/src/db/tts-quota-store.ts`, migration `0128`'s `app_private.release_tts_usage_reservation`, wired into every synthesis-failure path in `apps/api/src/routes/tts.ts`); the overlay display-queue reorder by `createdAt` in `apps/web/app/overlay/overlay-policy.ts`'s `selectPresentationGroup`.

**Explicitly out of scope, not touched:** RT-04/RT-05 (webhook commit, leased outbox dispatcher), RT-06 (histogram metrics — this task adds no new metric of any kind, not even a counter), RT-07 (OBS/device/load evidence), RT-08 (cron enablement), RT-13 (per-channel live-transport cap), Master Canvas/PRF-02, anything YouTube (`services/youtube-poller-go` untouched), any marketing or pricing copy, any tier or entitlement change, `DEFAULT_TTS_PLAYBACK_TIMEOUT_MS`, the §11.11 quota ladder (20K/40K/60K, unchanged), `app_private.get_overlay_events`'s unused `target_after_created_at`/`target_after_delivery_id` parameters (left exactly as `e8ca473` recorded them). No existing migration was modified, moved, or squashed; `0128` is a new forward migration only. `enrichment before release` (`Store.Release` called after `Enricher.Enrich`) is unchanged — §19.0 RT-03 states this ordering is deliberate and correct; this task adds classification, retry policy and quota-release around it, not a reordering.

## Design decisions this command did not settle

1. **`TtsQuotaMeter.release` is a required interface member**, not optional, unlike `TtsStore.storeFallbackReason` (which stays optional/back-compat). Reasoning: RT-03.6 is named a blocking acceptance test in the operator's command, and quota-charge correctness should not depend on every future caller remembering to implement an optional method. The three existing test files with pre-existing `TtsQuotaMeter` fakes (`apps/api/test/tts-quota-metering.test.ts`, `apps/api/test/l07-mute-synthesis-cost-gate.test.ts`, `apps/api/test/tts-fallback-reason.test.ts`) were mechanically updated (10 object literals, each gaining `async release() {}`) — no assertion in any of those pre-existing tests was changed.
2. **`app_private.release_tts_usage_reservation` operates on the existing `alert_tts_usage_monthly` accumulator column**, not a new ledger table. §10.2's grant/reserve/settle/release/refund/expiry vocabulary describes the AI-credits ledger conceptually; the TTS character quota's actual implementation (`0081`) is a per-channel/per-month counter, not literal ledger rows with an `entry_type` column. This task's release function decrements that counter (floored at zero via `greatest(...,0)`), which is a release over the existing mechanism, not a new one — consistent with the operator's instruction to follow the codebase's existing reserve/settle/release shape rather than invent a new entry type.
3. **A 5xx or a 429-with-`Retry-After` from the internal TTS route is classified retryable-unambiguous** on the reasoning that a complete HTTP response (status line and headers received) means the request definitely reached `apps/api`'s internal route and got a definitive answer, and — after this task's own quota-release fix — that answer is guaranteed to have already released any reservation before the error response left the route. This is an inference from the current single-hop worker→API topology (not a literal "5xx before any response body" byte-level distinction, which Go's `http.Client` does not expose at this call site) and is recorded as `Unverified` in the review.
4. **No new metric was added.** RT-06 (histograms) is explicitly out of scope for this task, and RT-02's precedent of adding bounded *counters* was not repeated here because the operator's command for this task lists only `Logger.Event` (not a metrics counter) under the privacy/security section for the Go side. If a bounded `tts_enrich` outcome counter is later wanted for operability, that is additive and does not require reopening this task.
5. **The register-map.tsv / semantic-mapping-audit.md `state` cell for `RT-03` is left at `X`**, matching `FULL-PRODUCT-DEFINITION.md` §31.18.0's own `RT-03` row, which this command explicitly says not to change. `lifecycle` moves from `new-record-required` to `active-record` since a real ten-field record now exists, mirroring RT-02's precedent exactly.
