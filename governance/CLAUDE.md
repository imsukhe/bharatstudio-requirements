# Claude Code instructions

Read `AGENTS.md` in this folder first. It is the canonical BharatStudio operating policy.

Before implementation, read the target repository's root `AGENTS.md`, root `CLAUDE.md`, the current approved requirement, task, acceptance record, and decision record. Stop for explicit user approval at the definition-to-implementation gate. Do not claim independent review when one did not occur. Preserve unrelated changes, never expose secrets, and use the central requirements repository as the authority.
